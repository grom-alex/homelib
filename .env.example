# HomeLib — шаблон переменных окружения
# Скопируйте в .env и заполните значения:
#   cp .env.example .env

# === Секреты (обязательно сменить в production!) ===

# Пароль PostgreSQL
DB_PASSWORD=homelib

# JWT секрет (минимум 32 символа)
# Сгенерировать: openssl rand -hex 32
JWT_SECRET=change-me-in-production-must-be-at-least-32-chars

# === Пути ===

# Путь к директории библиотеки (содержит .inpx и ZIP-архивы)
LIBRARY_PATH=/mnt/smb/media/MyHomeLib/lib.rus.ec

# === Опциональные ===

# Порт для Nginx (внешний)
NGINX_PORT=80
